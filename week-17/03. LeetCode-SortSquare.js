// arr
let arr= [-4,-1,0,3,10];
// square the array
arr = arr.map(x => x * x);
// apply either Merge Sort or Quick Sort to sort the squared array
arr = mergeSort(arr);
// Use the Example No:2 from Merge Sort
function mergeSort(arr){}